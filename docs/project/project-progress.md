# AiFiction 项目进度

最后更新：2026-03-26

## 1. 当前项目目标

当前项目目标已经收敛为：构建一个“本地正文驱动、自动维护优先、人工审查兜底”的个人网文控制台。

这意味着：
- 不再把重点放在手工录入型后台。
- 不把重点放在一开始就做复杂 Agent 协作。
- 优先把数据底座、增量同步、自动维护和审查机制做稳。

## 2. 当前所处阶段

当前阶段：**阶段 4 后半段收口**

阶段判断：
- V2 数据底座可以继续承载后续改造。
- 当前改造属于增量演进，不需要推倒重建数据库。
- 自动同步、待审查和首版事实回写已经接入 Web 工作台。
- 页面开始从“重手工后台”收缩成“自动维护控制台”。

## 3. 已完成内容

### 3.1 数据底座
- SQLite 已接入。
- V2 schema 已落地。
- 自动同步相关 V2 表已进入 schema 与 migration。
- `db:v2-smoke`、`db:sync-smoke` 已可稳定运行。

### 3.2 自动同步基础设施
- 已新增同步源仓储。
- 已新增同步工作流仓储。
- 已新增项目目录同步服务。
- 已新增审查队列服务。
- 已新增通过候选的事实回写服务。
- 已新增 Worker 目录同步入口。
- 已支持：列作品、列目录源、绑定目录、执行扫描。
- 已支持：对新增 / 修改文件生成摘要预览 artifact。
- 已支持：对新增 / 修改文件生成结构化抽取预览 artifact。
- 已支持：写入 `asset_updates_v2` 与 `review_queue_v2`。
- 已支持：对审查项执行“通过 / 驳回”并回写状态。
- 已支持：把通过的角色、关系、伏笔、时间线候选写入结构化事实层。
- 已支持：为未手工建章的源文档自动补占位卷 / 占位章节映射。

### 3.3 Web 工作台
- 首页可读取 V2 作品概览。
- 首页已收口为“作品总览 + 快速创建 + 帮助入口”。
- 作品详情页默认优先展示自动同步、待审查、资产概览和关系预览。
- 逐条编辑卷 / 角色 / 章节的入口已经收进折叠区。
- 手工维护兜底入口继续保留，但默认不打开展示。

## 4. 当前未完成的关键项

### P0
- 把启发式抽取预览升级成更真实的结构化抽取。
- 为事实回写补来源引用与冲突处理。
- 继续简化首页与详情页的交互，弱化手工维护。

### P1
- 角色关系图谱页。
- 事实级来源引用展示。
- 章节改动影响分析。
- Worker 主生成链逐步切到 V2。

## 5. 下一阶段建议顺序

1. 先补来源引用与冲突处理。
2. 再继续压缩首页和详情页中的手工入口。
3. 再做关系图谱与来源引用展示。
4. 最后把更真实的模型抽取和生成链逐步接进来。