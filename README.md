# AiFiction

AiFiction 是一个面向个人创作的长篇网文控制台。当前主线已经收敛为：**本地正文驱动、自动维护优先、人工审查兜底**。

## 当前定位

这个项目不是“全自动写小说的 Agent 平台”，而是一个可持续演进的小说工作台：
- 本地目录中的正文、设定和大纲持续推进。
- 系统负责增量扫描、结构化抽取、待审查整理和事实回写。
- Web 工作台主要负责查看、判断、修正和回溯，而不是承载大规模手工录入。

## 当前已具备的能力

- V2 SQLite 数据底座已经落地，并支持继续扩展。
- Worker 已具备目录同步入口，可列作品、列目录源、绑定目录并执行扫描。
- 新增或修改文件后，系统会自动生成：
  - 摘要预览 artifact
  - 结构化抽取预览 artifact
  - `asset_updates_v2`
  - `review_queue_v2`
- Web 工作台已经接入：
  - 作品书架
  - 快速创建作品
  - 目录绑定与重扫
  - 待处理变更包
  - 审查通过 / 驳回
- 通过审查的角色、关系、伏笔、时间线候选，已经可以首版写回结构化事实层。
- 首页已经进入“导航 + 书架 / 列表 + 搜索”的工作台形态。
- 作品详情页已经拆成“总览 / 待处理 / 剧情资产 / 高级维护”四个分区。

## 核心文档

- `docs/operations/workbench-operations-manual.md`
- `docs/architecture/system-design.md`
- `docs/project/project-progress.md`

## 常用命令

```powershell
npm.cmd run machine:context
npm.cmd run encoding:check
npm.cmd run dev:web
npm.cmd run dev:worker
npm.cmd run worker:sync -- --list-projects
npm.cmd run worker:sync -- --project demo-work-v2 --list-sources
npm.cmd run db:v2-smoke
npm.cmd run db:sync-smoke
npm.cmd run db:workbench-smoke
npm.cmd run typecheck
```

## 当前阶段

当前进入“阶段 4.5：产品收口与自动化减负”：
- 后端主链已经打通：本地目录同步 -> 抽取预览 -> 审查 -> 首版事实回写。
- 首页书架入口和详情页分区式工作台都已经落地。
- 当前最紧急的问题已经转向：进一步降低人工占比，继续收口审查与冲突处理，而不是重做底层数据库。

下一步优先做：
1. 继续强化来源引用和冲突处理。
2. 让低风险项尽可能自动通过。
3. 把影响分析逐步接成更明确的动作分发。

## 机器上下文

- 先运行 `node scripts/machine-context.mjs` 或 `npm.cmd run machine:context`，读取当前电脑的有效配置。
- 机器配置来自 `config/machine-profiles.json`，可选的本地覆盖文件是 `config/machine-overrides.local.json`。
- 这层配置统一管理仓库根路径、`git safe.directory`、GitHub SSH 主机或别名、默认 Web 端口和数据库路径。
- 切换电脑时，优先切换机器上下文，不再改 skill 里的固定路径。

## 中文与编码

- 仓库文本文件统一使用 UTF-8 和 LF 行尾。
- 修改中文文档和页面文案后，先跑 `node scripts/encoding-check.mjs` 或 `npm.cmd run encoding:check`。
- 如果 PowerShell 的默认输出或重定向会污染中文，改用显式 UTF-8 写入，或通过 Node 脚本落盘。

### 自动分流（2026-03-28）
- 待处理区新增“自动分流并清理低风险”入口。
- 系统会自动通过低风险审查项，并把剩余变更包继续分成“需要复核”和“存在冲突”。

## 最近更新（2026-03-28）
- 待处理页已经按“自动接收 / 需要复核 / 冲突处理”三条路径拆开显示，不再是一长串变更包列表。
- 每个变更包现在都会附带明确的处理策略，例如：自动写回、先核对证据、等待更多上下文、先解决事实冲突。
- 作品详情页的焦点区和统计区已改成更偏“任务优先”的视角，优先暴露冲突包、复核包和低风险自动包。

## 最近更新（2026-03-28 / 第二轮）
- 工作台现在会额外输出“建议复核章节”，用于标记受当前变更包影响的后续章节。
- reviewStats 已区分信息缺口、事实冲突、格式阻塞、置信度复核，不再只有总量和风险级别。
- 作品详情页和待处理页都已经把“建议复核章节”提到前台，便于你顺手做后续连贯性复核。

## 后续章节动作分流（2026-03-28）
- 工作台现在会把受当前变更包影响的后续章节分成两档：`正式复核` 和 `提醒回看`。
- 触发冲突包、事实冲突或被多个变更包重复命中的章节，会优先进入正式复核。
- 其余受影响章节先保留为提醒回看，避免把所有后续章节都抬成高优先级任务。

## 正式复核队列（2026-03-28）
- 作品详情页现在会把“正式复核章节”单独提成一个后续处理块，不再只藏在待处理页的建议章节分组里。
- 项目焦点也会优先提醒这类章节，避免它们在清理完变更包后被遗漏。

## 正式复核任务（2026-03-28）
- 项目级 follow-up queue 现在不再只是章节列表，而是会输出正式复核任务对象。
- 详情页正式复核区已经按任务展示，后续如果要接真正的章节级复核动作，不需要再重排读模型。

## 最近更新（2026-03-28 / 正式复核任务状态）
- 已为正式复核章节任务新增持久化状态，支持按章节记录“待处理 / 处理中 / 已完成 / 本轮跳过”。
- 详情页里的“正式复核队列”现在可以直接标记已复核或本轮跳过，并且会影响后续工作台读模型的活跃任务列表。
- 新增迁移：`packages/data/drizzle/0002_numerous_ikaris.sql`。

## 最近更新（2026-03-28 / 关系图谱视图）
- 作品详情页新增“关系图谱”独立视图，把角色网络、关系事实和来源证据拆开展示。
- 这是关系图谱的第一版落地：先解决“看不清、挤在一页里”的问题，后续再继续增强图谱交互。

- 关系图谱页已支持按角色和来源类型筛选，刷新或切换视图时会保留筛选状态。
- 关系图谱和来源引用现在可以直接跳到同源变更包，便于从事实层反查到对应章节。
- 正式复核任务现在支持持久化处理结果，包含复核通过、需要修订、需要重跑抽取和本轮跳过，并会保留最近处理记录。

## 最新状态（2026-03-28）
- 同步主链已补上自动分流：扫描后会自动处理本次 sync run 内的低风险审查项。
- 低风险项会直接走审批与事实回写；高风险项继续留在工作台。
- 按当前主线口径，「来源追踪闭环 / 复核机制闭环 / 自动维护链闭环」已完成，项目进入首个可自用闭环。
